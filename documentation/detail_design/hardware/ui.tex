% !TEX root = main.tex

\subsection{User Interface}
\label{sec:hardware_ui}

The hardware for the user interface is the laptop on which the central processing is done. Users will be able to use the laptop's mouse and keyboard to specify parameters of operation. The laptop's monitor will be used to observe the workers' progress \added[remark={NJ, V2}]{and view any errors during operation}.